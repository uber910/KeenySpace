Golden fixture 05: three WAL fragments targeting three distinct pages.

Bucket: golden. Labeler: deployer (KeenySpace v1 eval baseline).

Scenario: Three WAL entries cover three distinct topics: WAL concurrency strategy,
Postgres schema design, and CLI architecture. The vault has only notes/index.md.
The compile agent should produce three PageOps: one for each distinct topic area.
